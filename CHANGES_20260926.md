# 修正内容 (2026-09-26)

## 1. 敵が復活(respawn)するたびに重くなる問題 → 修正
`client/systems/entitySetup.js`

敵が生成されるたびに `scene.physics.add.collider(...)` / `scene.physics.add.overlap(...)` で
Collider を作っていたが、敵が倒されて `destroy()` された後もその Collider が
物理ワールドに残り続けていた（一切破棄していなかった）。
そのため敵が respawn する（＝新しい敵オブジェクトが作られる）たびに Collider が
無限に積み重なり、毎フレームの当たり判定コストがプレイ時間とともに増加し、
「respawnするたびに重くなる」状態になっていた。

→ 生成した Collider を配列に保持し、敵の `destroy` イベントで全て `.destroy()` するように修正。

## 2. 敵の攻撃速度・攻撃力が高すぎる問題 → 修正
- `client/systems/entitySetup.js`: サーバー管理の敵は、サーバーAIが `enemyAttack` イベントで
  既に攻撃ダメージを送ってきているにもかかわらず、クライアント側の接触(overlap)ダメージでも
  同時にダメージを与えてしまっていた（二重ダメージ）。サーバー管理の敵についてはこの接触ダメージを無効化。
- `server/config.js` / `server/services/enemyLoop.js`: 敵の攻撃はAIの行動決定間隔(300ms)がそのまま
  攻撃間隔になっており、1秒に3回以上攻撃してくることがあった。`ENEMY_ATTACK_COOLDOWN_MS` (1200ms) を
  新設し、意思決定間隔とは独立した攻撃クールダウンとして管理するように変更。
- `server/config.js` / `server/data/enemyStats.js`: `ENEMY_ATK_SCALE` (0.6) を新設し、
  全敵の攻撃力に一括で係数を掛けられるようにした（バランス調整はこの1箇所を変えるだけでOK）。

## 3. 経験値が思ったより貯まらない問題 → 修正
`client/entities/Player.js`

レベルアップに必要な経験値 `maxExp` が `maxExp *= 1.5` という複利計算になっており、
レベル100までに 1.5^99 倍という天文学的な量が必要になっていた
（実質どれだけ敵を倒しても経験値バーがほとんど進まないように見えるバグ）。
`calcMaxExp(level) = 100 * level^1.8` という多項式カーブに変更し、終盤でも現実的な必要量に収めた。

## 4. 敵のレベル表示 → 追加
- `server/data/enemyStats.js`: 各敵タイプに `level` を追加（slime:1 ～ dragon_boss:100）。
- `server/services/enemyService.js`: spawn時に `level` を敵オブジェクトに含めてクライアントへ送信。
- `client/entities/Enemy.js`: 名前ラベルを `Lv.{level} {表示名}` 形式に変更。

## 5. 武器・防具の追加
`client/data/items.js` / `client/data/shops.js`
武器7種（銀の短剣, 戦斧, 秘術の杖, 雷鳴の槍, 月光のレイピア, 深淵の大剣, 不死鳥の弓）と
防具7種（鎖帷子, 騎士の鎧, 守護者のプレート, 神秘のローブ, タイタンアーマー, 天蓋の鎧, 虚無の護盾）を追加し、
既存のlvlReqの隙間（8, 10, 20, 30, 35, 55, 60, 70, 80, 95）を埋めた。すべて街の武器屋/防具屋で購入可能。

## 6. 運否天賦の杖（ATK 1〜150変動）
`client/data/items.js` の `ganble_stick.calculateAtk` と `client/entities/Player.js` の
`getDamage()` を確認したところ、既に `Math.floor(Math.random()*150)+1` が正しく呼び出され、
説明通り 1〜150 の間で変動するように実装されていた。動作確認済みのため変更なし。
